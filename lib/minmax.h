
#ifndef _MINMAX_H_
#define _MINMAX_H_

#ifndef MIN
#define MIN(x,y) ((x) < (y) ? (x) : (y))
#endif /*MIN*/

#ifndef MAX
#define MAX(x,y) ((x) > (y) ? (x) : (y))
#endif /*MAX*/

#endif /*_MINMAX_H_*/

